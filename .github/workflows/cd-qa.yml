name: CI-CD Pipeline (Build, Test, Deploy)
on:
  push:
    branches:
      - master
      - QA
  pull_request:
    branches:
      - master
permissions:
  contents: read
  actions: read
env:
  DOCKER_USER: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKER_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
  IMAGE_NAME: bp-calculator
  FULL_IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/bp-calculator:latest
  AZURE_WEBAPP_NAME: bp-calculator-qa-cst
  AZURE_WEBAPP_STAGING: bp-calculator-prod-cst
  AZURE_WEBAPP_PROD: bp-calculator-prod-cst
  AZURE_SLOT_NAME: blue
jobs:
  ###########################################################
  # CI — Build, Tests, Coverage, Security Scan
  ###########################################################
  build-test-sonar:
    name: CI - Build, Tests, Coverage, SonarCloud
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Install Tools
        run: |
          dotnet tool install --global dotnet-sonarscanner
          dotnet tool install --global coverlet.console
          echo "$HOME/.dotnet/tools" >> $GITHUB_PATH
      - name: Restore
        run: dotnet restore
      - name: SonarCloud Begin
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_PROJECT_KEY: ${{ secrets.SONAR_PROJECT_KEY }}
          SONAR_ORG: ${{ secrets.SONAR_ORG }}
        run: |
          dotnet-sonarscanner begin \
            /k:"${SONAR_PROJECT_KEY}" \
            /o:"${SONAR_ORG}" \
            /d:sonar.host.url="https://sonarcloud.io" \
            /d:sonar.token="${SONAR_TOKEN}" \
            /d:sonar.cs.opencover.reportsPaths="TestReports/coverage.opencover.xml"
      - name: Install Trivy
        run: |
          sudo apt-get update -y
          sudo apt-get install wget apt-transport-https gnupg lsb-release -y
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
          echo "deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" | sudo tee /etc/apt/sources.list.d/trivy.list
          sudo apt-get update -y
          sudo apt-get install trivy -y
      - name: Run Trivy Scan
        run: trivy fs --severity HIGH,CRITICAL --exit-code 1 --no-progress .
      - name: Build
        run: dotnet build --configuration Release --no-restore
      - name: Publish
        run: dotnet publish BPCalculator/BPCalculator.csproj --configuration Release --output ./published-app
      - name: Run Unit + BDD Tests
        run: |
          mkdir -p TestReports
          dotnet test BPCalculator.Tests/BPCalculator.Tests.csproj \
            --configuration Release \
            --logger "trx;LogFileName=test_results.trx" \
            --no-build \
            --results-directory TestReports
      - name: Coverage Report
        run: |
          mkdir -p TestReports
          coverlet \
            BPCalculator.Tests/bin/Release/net9.0/BPCalculator.Tests.dll \
            --target "dotnet" \
            --targetargs "test BPCalculator.Tests/BPCalculator.Tests.csproj --no-build --configuration Release" \
            --format opencover \
            --output TestReports/coverage.opencover.xml
      - name: SonarCloud End
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: dotnet-sonarscanner end /d:sonar.token="${SONAR_TOKEN}"
      - uses: actions/upload-artifact@v4
        with:
          name: published-app
          path: published-app/

  ###########################################################
  # CD — Download Artifact (QA)
  ###########################################################
  download:
    name: CD - Download Build Artifact
    runs-on: ubuntu-latest
    needs: build-test-sonar
    if: github.ref == 'refs/heads/QA'
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: published-app
          path: ./published-app

  ###########################################################
  # CD — Build & Push Docker Image
  ###########################################################
  docker:
    name: CD - Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: download
    if: github.ref == 'refs/heads/QA'
    steps:
      - uses: actions/checkout@v4
      - name: Login to Docker Hub
        run: echo "${DOCKER_TOKEN}" | docker login -u "${DOCKER_USER}" --password-stdin
      - name: Build Image
        run: docker build -t $FULL_IMAGE .
      - name: Push Image
        run: docker push $FULL_IMAGE

  ###########################################################
  # CD — Deploy to QA
  ###########################################################
  deploy:
    name: CD - Deploy to QA
    runs-on: ubuntu-latest
    needs: docker
    if: github.ref == 'refs/heads/QA'
    steps:
      - uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_QA }}
          images: ${{ env.FULL_IMAGE }}
      - name: Health Check QA
        run: |
          URL="https://bp-calculator-qa-cst-bverd2gge3dwftec.norwayeast-01.azurewebsites.net/health"
          for i in {1..15}; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" $URL)
            if [ "$STATUS" == "200" ]; then exit 0; fi
            sleep 5
          done
          exit 1

  ###########################################################
  # CD — Selenium E2E Tests
  ###########################################################
  e2e-tests:
    name: CD - Run E2E Selenium Tests
    runs-on: ubuntu-latest
    needs: deploy
    if: github.ref == 'refs/heads/QA'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Install Chrome + Driver
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser chromium-chromedriver
      - name: Run E2E Tests
        run: |
          mkdir -p BPCalculator.E2E/TestResults
          dotnet test BPCalculator.E2E/BPCalculator.E2E.csproj \
            --logger "trx;LogFileName=e2e_results.trx" \
            --results-directory BPCalculator.E2E/TestResults/
      - uses: actions/upload-artifact@v4
        with:
          name: e2e-results
          path: BPCalculator.E2E/TestResults/

  ###########################################################
  # CD — Load Test using k6
  ###########################################################
  load-test:
    name: CD - Load Test (k6)
    runs-on: ubuntu-latest
    needs: e2e-tests
    if: github.ref == 'refs/heads/QA'
    steps:
      - uses: actions/checkout@v4
      - name: Install K6
        run: |
          sudo apt-get update
          sudo apt-get install -y gnupg2 ca-certificates
          curl -fsSL https://dl.k6.io/key.gpg | sudo gpg --dearmor -o /usr/share/keyrings/k6-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install -y k6
      - name: Run Load Test
        run: k6 run loadtest.js

  ###########################################################
  # CD — OWASP ZAP Security Scan
  ###########################################################
  zap-scan:
    name: CD - OWASP ZAP Scan
    runs-on: ubuntu-latest
    needs: load-test
    if: github.ref == 'refs/heads/QA'
    steps:
      - uses: actions/checkout@v4
      - name: Run ZAP
        run: |
          chmod +x zap-scan.sh
          ./zap-scan.sh
      - uses: actions/upload-artifact@v4
        with:
          name: zap-security-report
          path: zap-output/

  ###########################################################
  # CD — Deploy to STAGING (Blue Slot)
  ###########################################################
  deploy-staging:
    name: CD - Deploy to Staging Slot (Blue)
    runs-on: ubuntu-latest
    needs: zap-scan
    if: github.ref == 'refs/heads/QA'
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Staging Slot
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.AZURE_WEBAPP_STAGING }}
          slot-name: ${{ env.AZURE_SLOT_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_BLUE }}
          images: ${{ env.FULL_IMAGE }}
      - name: Staging Health Check
        run: |
          URL="https://bp-calculator-prod-cst-blue-g9e6dabjadbgd6ct.norwayeast-01.azurewebsites.net/health"
          for i in {1..15}; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" $URL)
            if [ "$STATUS" == "200" ]; then exit 0; fi
            sleep 5
          done
          exit 1

  ###########################################################
  # CD — Deploy to PRODUCTION (Manual Approval Required)
  ###########################################################
  deploy-prod:
    name: CD - Deploy to Production
    runs-on: ubuntu-latest
    needs: deploy-staging
    environment: production
    if: github.ref == 'refs/heads/QA'
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Production
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.AZURE_WEBAPP_PROD }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_PROD }}
          images: ${{ env.FULL_IMAGE }}
      - name: Production Health Check
        run: |
          URL="https://bp-calculator-prod-cst-hgb9ggfqdnfrf8g8.norwayeast-01.azurewebsites.net/health"
          for i in {1..15}; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" $URL)
            if [ "$STATUS" == "200" ]; then exit 0; fi
            sleep 5
          done
          exit 1